- id: gw-flow-consistency-001
  scene: ABACUS + LibRPA GW workflow
  symptom: LibRPA input parsing/read failure or inconsistent outputs
  root_cause: Inputs assembled from mixed directories or stale outputs remain
  fix:
    - Regenerate librpa inputs from the same ABACUS workflow outputs
    - On ABACUS `master_ghj`, set `out_librpa_reader_version 1` for GW producer runs
    - In pinned LibRPA `v0.7.0`, set reader-v1 prefixes and selectors: `prefix_coul_full = v1_coulomb_full_iq_`, `prefix_coul_cut = v1_coulomb_cut_iq_`, `prefix_lri_coeff = v1_Cs_data_`, `prefix_lri_coeff_shrink = v1_Cs_shrinked_data_`, `prefix_shrink_sinvS = v1_shrink_sinvS_`, `fn_basis_wfc = basis_wfc_out`, `fn_basis_aux = basis_aux_out`, and `fn_basis_aux_shrink = basis_aux_shrink_out` when `use_shrink_abfs = t`, `version_coul_reader = 1`, and `version_lri_reader = 1`
    - Run in a fresh directory
    - Remove stale output artifacts before rerun
    - Prefer a user-curated server-side reference bundle when available
  verify:
    - Confirm source paths for SCF/DF/NSCF are from one run chain
    - Confirm `INPUT_scf` contains `out_librpa_reader_version 1`
    - Confirm `librpa.in` contains reader-v1 prefixes plus `version_coul_reader = 1` and `version_lri_reader = 1`
    - Re-run minimal smoke with nfreq=16
  applies_to:
    abacus: ">=3.x"
    librpa: ">=0.x"

- id: shrink-abfs-coupling-002
  scene: default GW workflow with shrink enabled
  symptom: unstable/failed run due to missing or inconsistent shrink-related thresholds
  root_cause: shrink_abfs should be enabled by default, but companion parameters are missing or misunderstood
  fix:
    - Set `use_shrink_abfs = t`
    - Set `rpa 1`
    - Set `exx_pca_threshold 10`
    - Set `shrink_abfs_pca_thr 1e-4`
    - Set `shrink_lu_inv_thr 1e-3`
    - Set `exx_cs_inv_thr 1e-5`
    - Require the user to specify `ABFS_ORBITAL` in `STRU`
    - If the user provides `.abfs` files, use those filenames directly in `ABFS_ORBITAL`
  verify:
    - Check all coupled parameters exist and are parsed correctly
    - Confirm shrink is enabled explicitly in `librpa.in`
    - Confirm `ABFS_ORBITAL` exists in `STRU`
    - Confirm referenced `.abfs` filenames exist in the working directory or specified path
    - Perform short smoke test first
  applies_to:
    mode: gw

- id: shrink-parameter-meaning-003
  scene: user asks how shrink defaults should be interpreted
  symptom: parameters are present but their roles are unclear
  root_cause: shrink-related defaults encode different controls for compressed basis size, inversion stability, and external-input routing
  fix:
    - Treat `shrink_abfs_pca_thr = 1e-4` as the default control on compressed auxiliary-basis size
    - Treat `shrink_lu_inv_thr = 1e-3` as the default control for inversion-error handling in output `sinvS`
    - Treat `exx_pca_threshold = 10` as the switch meaning the large-basis path expects external input when shrink is enabled
  verify:
    - Explain each parameter role before changing it
    - Only tune one shrink-related threshold at a time
  applies_to:
    mode: gw

- id: periodic-gw-symmetry-004
  scene: periodic ABACUS + LibRPA GW case with symmetry enabled
  symptom: LibRPA misses symmetry reduction or disagrees with the intended symmetry-on/off comparison setup
  root_cause: input symmetry was not enabled consistently across SCF, NSCF, LibRPA, and the current `stru_out` handoff
  fix:
    - Set `INPUT_scf` to `rpa 1` and `symmetry 1`
    - Keep `INPUT_nscf` and any standalone band `INPUT` on `symmetry -1`
    - Set `use_symmetry_exx = t` and `use_symmetry_gw = t` in `librpa.in`; reject old OML symmetry spellings because LibRPA 0.7.0 does not parse them as aliases
    - Require `stru_out` from the same symmetry-enabled SCF; current LibRPA rebuilds rotations from the symmetry metadata in that file
    - Do not require or copy legacy `irreducible_sector.txt`, `symrot_R.txt`, `symrot_k.txt`, or `symrot_abf_k.txt`
    - Keep all non-symmetry parameters identical when comparing symmetry-on and symmetry-off runs
  verify:
    - Confirm `stru_out` exists after SCF in the symmetry lane
    - Confirm `INPUT_nscf` still uses the full-k setup with `symmetry -1`
    - Confirm `librpa.in` enables both EXX and GW input symmetry flags
    - When a LibRPA log is available, confirm it reports k rotations generated from symmetry operations and legacy sidecars ignored
  applies_to:
    mode: gw
